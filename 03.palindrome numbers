#
# @lc app=leetcode id=9 lang=python3
#
# [9] Palindrome Number
#

# @lc code=start
class Solution:
    def isPalindrome(self, x: int) -> bool:
        a=str(x)
        b=a[::-1]
        if(a==b):
            return True
        else:
          return  False


        
# @lc code=end

logic without converting to string:
num = 121
a = num
b = 0

while num > 0:
    digit = num % 10
    b = b * 10 + digit
    num = num // 10

if (a == b):
    return true
else:
    return false
